# خارطة طريق تحسين خدمة زبوني

تحليل بعد فحص كامل للـ API (~10,000 سطر في `artifacts/api-server/src/routes/*.ts`) وبعد أعطال 7 تموز 2026 المباشرة. مرتّب بالأولوية.

> **الحالة عند كتابة التقرير:** الخدمة شغّالة، ومشكلة الـ OTP انحلّت بإضافة عمود `zone_id` (nullable) إلى جدولي `users` و`restaurants` في `marsool_db`.

---

## ما كشفته أعطال اليوم

| العَرَض | السبب الجذري | الدرس |
|---|---|---|
| 502 على كل طلبات الأدمن | `listen_addresses` في postgres يشير لبوابة شبكة خاطئة → حلقة تعطّل | لا فحص صحة يتحقق من قاعدة البيانات فعلياً |
| كود التحقق (OTP) يُرفض / 500 | الكود المنشور يستعلم عن عمود `zone_id` غير موجود في `marsool_db` | الترحيلات لا تُطبَّق بشكل موثوق على الإنتاج |
| السيرفر يسقط | استعلام غير محمي داخل مهمّة دورية → unhandled rejection (أُصلح) | نمط `void job()` خطير بلا معالج شامل |

---

## 🔴 P0 — موثوقية وبنية تحتية

### 1. الترحيلات (migrations) دوال يدوية تبتلع أخطاءها بصمت
`artifacts/api-server/src/index.ts`
كل ترحيل يُستدعى بنمط `addX().catch(log)`. لو فشل أو كان ناقصاً، السيرفر يكمل بمخطط قاعدة بيانات غير متطابق → أخطاء 500 وقت التشغيل (بالضبط ما حصل مع `zone_id`). لا جدول تتبّع، ولا ترتيب مضمون. وملاحظة: `add-courier-monthly-subscriptions` موجود في الحزمة لكنه **لا يُستدعى إطلاقاً** عند الإقلاع.
**الحل:** ترحيلات `drizzle-kit` بجدول journal متتبّع، تُشغَّل كخطوة نشر **تفشل النشر عند أي خطأ** بدل ابتلاعه + فحص schema drift في CI.

### 2. فحص الصحة لا يتحقق من قاعدة البيانات
`artifacts/api-server/src/routes/health.ts`
`/healthz` يرجّع `{status:"ok"}` ثابتة دائماً. الحاوية تعلن أنها سليمة حتى لو قاعدة البيانات مقطوعة.
**الحل:** اجعل `/healthz` يشغّل `SELECT 1` فعلياً (readiness) وافصله عن فحص الحياة (liveness).

### 3. ازدواجية في طوبولوجيا قاعدة البيانات
`docker-compose.yml`
الـ compose يشغّل حاوية `postgres`، بينما الإنتاج يتجاوزها عبر `DATABASE_URL` إلى postgres آخر على المضيف (`marsool_db`). حاوية بلا فائدة + التباس حول القاعدة الحيّة.
**الحل:** اختر مصدراً واحداً، ووثّق الطوبولوجيا (5432 زبوني مقابل 5433 فيتوباس).

### 4. لا حارس للانهيار على مستوى العملية
أي `void someJob()` بلا `.catch` شامل قد يكرّر انهيار اليوم.
**الحل:** معالج عام لـ `unhandledRejection` و`uncaughtException` + نمط `.catch` على كل مهمّة دورية.

### 5. لا مراقبة ولا تنبيهات
كل أعطال اليوم اكتُشفت يدوياً.
**الحل:** مراقب uptime على `/healthz` + تنبيه عبر `ADMIN_ALERT_WEBHOOK_URL` (موجود أصلاً وغير مستعمل).

### 6. هشاشة إدارة أسرار البيئة
أمر إعادة تشغيل خاطئ قد يمسح `DATABASE_URL`. الأسرار في `.env` بلا نسخة احتياطية موثّقة.
**الحل:** `.env` مصدر وحيد + نسخة احتياطية مشفّرة خارج السيرفر + توثيق أمر إعادة التشغيل الآمن.

---

## 🟠 P1 — مالية وأمان

### 7. نقاط الولاء تُحرَق دون خصم فعلي من السعر 💸
`routes/orders.ts` — `POST /orders` (~537–673)
عند `usePoints` تُحرق النقاط ويُرجَع `pointsDiscount`، لكن لا مكان يطرح قيمة الخصم من `totalPrice`. مع الدفع عند الاستلام، المندوب يأخذ السعر كاملاً → الزبون يخسر نقاطه بلا مقابل.
**الحل:** اطرح `discountAmount` من سعر الطلب داخل نفس الترانزاكشن، أو امنع الطلب إن لم ينطبق الخصم.

### 8. خصم المحفظة يُحسب قبل تطبيق العرض السريع 💸
`routes/orders.ts` — `POST /orders` (~523–625)
`deductAmount` يُحسب على السعر الأصلي قبل الترانزاكشن، بينما flash deal يُطبَّق لاحقاً ويخفّض السعر → خصم أكبر من قيمة الطلب الفعلية.
**الحل:** احسب خصم المحفظة **بعد** كل الخصومات، واحصره بسقف السعر النهائي.

### 9. أكواد OTP احتياطية ثابتة (Backdoor) 🔓
`routes/auth.ts` — `verify-otp`
`"000000"` و`"999999"` مقبولة لأي رقم عندما `NODE_ENV !== "production"`. نسيان ضبط `NODE_ENV` في بيئة فيها بيانات حقيقية = استيلاء على أي حساب بمعرفة رقم الهاتف.
**الحل:** اربطها بمتغيّر بيئة صريح منفصل (مثل `ALLOW_TEST_OTP`) واحذفها من صورة الإنتاج.

### 10. حذف مطعم من الأدمن بلا حماية
`routes/admin.ts` — `DELETE /admin/restaurants/:id`
حذف cascade مباشر بلا فحص طلبات نشطة وبلا تأكيد.
**الحل:** امنع الحذف عند وجود طلبات `searching/accepted`، اطلب تأكيداً، أو استخدم soft-delete.

### 11. تعديل حالة الطلب بلا تحقّق منطقي
`routes/admin.ts` — `PATCH /admin/orders/:id/status`
يقبل أي حالة ويكتبها (مثلاً إرجاع `delivered` إلى `searching`) مع إطلاق إشعارات.
**الحل:** اسمح فقط بالانتقالات المنطقية عبر state machine.

---

## 🟡 P2 — سلامة البيانات وحالات التسابق

### 12. ثغرات تسابق تتجاوز فحوص «مرّة واحدة»
`courier.ts` · `orders.ts`
مندوب قد يأخذ طلبين نشطين معاً؛ سقف كود الخصم قابل للتجاوز؛ تقييم/إحالة مكرّرة — كلها تعتمد على `count()` قبل الترانزاكشن.
**الحل:** قيود `UNIQUE` على `(promoId, userId)` و`(orderId)` للتقييم، وشرط حالة ذرّي ضمن نفس `UPDATE` لقبول المندوب.

### 13. مسارات الحذف تُرجع نجاحاً لسجلّ غير موجود
`admin.ts` — عدة مسارات `DELETE` ترجّع 204 حتى لو الـ id غير موجود.
**الحل:** أرجِع 404 عند عدم وجود السجل، ووحّد النمط.

### 14. CORS مفتوح على Socket.io
`artifacts/api-server/src/index.ts` — `cors: { origin: "*" }`.
**الحل:** اقصر `origin` على نطاقات زبوني الرسمية.

---

## ✅ ابدأ من هنا — أول 5 خطوات (أعلى أثر / أقل جهد)

1. اجعل `/healthz` يتحقق من قاعدة البيانات — نصف ساعة، يكشف الأعطال قبل المستخدم.
2. حوّل الترحيلات إلى خطوة نشر تفشل عند الخطأ + فحص drift في CI — يمنع تكرار مشكلة `zone_id`.
3. اربط مراقب uptime بتنبيه عبر `ADMIN_ALERT_WEBHOOK_URL` — لتعرف قبل زبائنك.
4. أصلح خصم نقاط الولاء وترتيب خصم المحفظة/العرض — خسارة مالية مباشرة في كل طلب.
5. افصل أكواد OTP الاحتياطية عن `NODE_ENV` — يغلق باب الاستيلاء على الحسابات.
